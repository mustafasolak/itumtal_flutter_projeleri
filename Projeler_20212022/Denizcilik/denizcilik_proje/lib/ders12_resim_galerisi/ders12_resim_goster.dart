import 'package:flutter/material.dart';

class Ders12_Resim_Goster extends StatelessWidget {
  const Ders12_Resim_Goster({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Resim Gösterici"),
      ),
      body: Text("hoşgeldiniz"),
    );
  }
}
