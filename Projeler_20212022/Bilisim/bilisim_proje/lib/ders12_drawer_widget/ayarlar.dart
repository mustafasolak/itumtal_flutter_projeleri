import 'package:flutter/material.dart';

class Ders12_Ayarlar extends StatelessWidget {
  const Ders12_Ayarlar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        actions: [],
        title: Text(
          "Ayarlar",
        ),
      ),
      body: Text("Ayarlar sayfası"),
    );
  }
}
