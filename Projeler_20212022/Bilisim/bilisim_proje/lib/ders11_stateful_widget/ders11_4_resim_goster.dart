import 'package:flutter/material.dart';


class ResimGosterici extends StatelessWidget {
  const ResimGosterici({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Text("selamm"),
      ),
    );
  }
}
