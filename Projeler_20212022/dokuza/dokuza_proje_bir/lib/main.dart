import "package:flutter/material.dart";

void main() {
  runApp(
    Center(
      child: Text(
        "Merhaba 9-A Sınıfı selam",
        textDirection: TextDirection.ltr,
        style: TextStyle(
          color: Colors.greenAccent,
          fontSize: 30.0,
          fontWeight: FontWeight.bold,
        ),
      ),
    )
  );
}
